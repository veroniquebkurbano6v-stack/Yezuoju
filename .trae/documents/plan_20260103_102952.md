## 修复计划

### 问题分析
从日志可以看到：
- `has_retrieved: True, has_matched: True`（应该进入主流程）
- `[DeepSeekAgent] 进入主流程（检索回答）`（确实进入了主流程）
- 但 `final_answer` 为空，返回了提示信息

这说明问题可能在于：
1. 代码意外进入了历史回答分支（`if not has_retrieved or not has_matched` 判断有问题）
2. 或者在历史回答分支中失败了

### 修复方案

**文件**: `d:\PyCharm 2025.1\project\StoryRag v2.0\src\deepseek_agent.py`

**修改内容**:

1. **在历史回答分支入口**（第170行）添加日志：
   ```python
   logger.info(f"[DeepSeekAgent] 检查是否进入历史分支: not has_retrieved={not has_retrieved}, not has_matched={not has_matched}")
   ```

2. **在历史分支的 LLM 调用失败后**添加日志，记录是否设置 `answer_source = "none"`

3. **在 if-else 块结束后**（第209行）添加日志，确认 `final_answer` 的值：
   ```python
   logger.info(f"[DeepSeekAgent] 历史分支处理完成，final_answer={final_answer is not None}, answer_source={answer_source}")
   ```

### 预期效果
通过这些日志，可以确认：
1. 是否意外进入了历史回答分支
2. 在历史分支中发生了什么
3. 为什么 `final_answer` 最终为空