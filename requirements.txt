# StoryRag v2.0 项目依赖
# 使用国内镜像安装：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# =============================================================================
# PyTorch & Deep Learning
# =============================================================================
# PyTorch CUDA 版本 (GPU 加速，根据显卡选择)
# RTX 40 系列：pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu124
# ⚠️ 注意：torch 2.7.x 存在兼容性问题，建议使用 2.5.x 或更低版本
torch>=2.5.0,<2.7.0
torchaudio>=2.5.0,<2.7.0
torchvision>=0.20.0,<0.22.0

# Transformers & Sentence Transformers
transformers>=4.39.0
sentence-transformers>=2.7.0
accelerate>=0.28.0
safetensors>=0.4.0

# =============================================================================
# LangChain & Agent Framework
# =============================================================================
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langgraph>=0.2.0  # Agent 编排框架

# =============================================================================
# Vector Database & Retrieval
# =============================================================================
chromadb>=0.4.0  # 向量数据库（核心依赖）
faiss-cpu>=1.7.0  # 轻量级向量检索工具
# pinecone-client>=2.2.0  # 如需使用请取消注释

# =============================================================================
# Document Processing
# =============================================================================
# PDF 处理
PyPDF2>=3.0.0
pypdf>=4.0.0
pdfplumber>=0.11.0
PyMuPDF>=1.19.0  # fitz
pdfminer.six>=20221105
pdf2image>=1.16.3
pytesseract>=0.3.10  # OCR
opencv-python>=4.9.0  # 图像处理

# 其他文档格式
python-docx>=1.1.0

# =============================================================================
# Data Processing & Scientific Computing
# =============================================================================
numpy>=1.24.0,<2.0.0  # 与 uqlm 等包兼容
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
tiktoken>=0.12.0  # Token 计数工具

# =============================================================================
# Configuration & Settings
# =============================================================================
pydantic-settings>=2.0.0  # 配置管理

# =============================================================================
# Web Framework & API
# =============================================================================
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.27.0
requests>=2.31.0
aiofiles>=23.0.0  # 异步文件操作（会话管理器依赖）

# =============================================================================
# Configuration & Environment
# =============================================================================
python-dotenv>=1.0.0
pydantic>=2.5.0

# =============================================================================
# Logging & Utilities
# =============================================================================
orjson>=3.9.0  # 快速 JSON 序列化

# =============================================================================
# Optional Dependencies (按需启用)
# =============================================================================
# jieba>=0.42.0  # 中文分词
# spacy>=3.7.0  # NLP 处理
# matplotlib>=3.8.0  # 可视化
# seaborn>=0.13.0  # 统计可视化

# =============================================================================
# Development Tools (开发环境)
# =============================================================================
# pytest>=7.4.0  # 测试框架
# black>=23.0.0  # 代码格式化
# flake8>=6.0.0  # 代码检查

# =============================================================================
# RAG Evaluation (RAG 评测)
# =============================================================================
ragas>=0.1.0  # RAG 系统标准化评测框架
langchain-openai>=0.1.0  # RAGAS 需要 OpenAI 兼容接口（可使用 DeepSeek）
datasets>=2.14.0  # Hugging Face 数据集（RAGAS 依赖）
nest-asyncio>=1.5.0  # 支持异步事件循环

# =============================================================================
# DeepSeek Evaluation (DeepSeek 评测工具)
# =============================================================================
# DeepSeek API 调用（使用 openai 兼容接口）
# 通过 langchain-openai 调用 DeepSeek，无需额外依赖

# 批量评测和监控工具
# tqdm>=4.66.0  # 进度条显示（可选）
